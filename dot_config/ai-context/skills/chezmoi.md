# Chezmoi

Use when working on Pi config, extensions, themes, or dotfiles.

Do not load this skill unless the task actually touches dotfiles or Pi config.

## Source of truth

Read and follow:

- `~/.config/ai-context/environment/chezmoi.md`

## Apply it when working in

- `~/.local/share/chezmoi/` — chezmoi source
- `~/.pi/agent/` — deployed Pi config
