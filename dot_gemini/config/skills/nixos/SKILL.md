---
name: nixos
description: NixOS configuration and system management. Load only for NixOS modules, packages, hosts, or flake config.
---

# NixOS Skill (Antigravity Adapter)

Canonical skill lives in:

- `~/.config/ai-context/skills/nixos.md`

Read that file first, then apply its rules when working in:

- `~/dotfiles/nixos/`
