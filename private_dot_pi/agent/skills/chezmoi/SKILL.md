---
name: chezmoi
description: Pi dotfiles management and chezmoi setup. Use when working on pi config, extensions, themes, or dotfiles.
---

# Chezmoi Skill (Pi Adapter)

Canonical documentation lives in:

- `~/.config/ai-context/environment/chezmoi.md`

Read that file first, then apply its rules when working in:

- `~/.local/share/chezmoi/` (chezmoi source)
- `~/.pi/agent/` (deployed Pi config)

If Pi-specific behavior is needed, also check:

- `~/.pi/agent/AGENTS.md`
